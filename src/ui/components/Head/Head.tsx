import { HeaderContainter, Logo } from './head.style'

const Header = () => {
  return (
    <HeaderContainter>
      <Logo src='/images/logo.svg' alt='Logo' />
    </HeaderContainter>
  )
}
export default Header